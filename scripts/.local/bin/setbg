#!/bin/sh

[ -f "$1" ] && wall="$1"

[ -z "$1" ] && wall=$(find ~/.config/wallpaper/wallpaper | shuf -n 1)

cp $wall ~/.cache/wall.png

notify-send -i "$wall" "Wallpaper changed."

wal -i $wall

cd $HOME/.config/i3/
./xres-bar
cd -

i3-msg restart
